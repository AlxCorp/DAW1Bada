EC2:
IP ESTATICA
CREAR CLAVE

ssh -i clave.pem ubuntu@IP

sudo apt update
sudo apt install apache2 php libapache2-mod-php php-mysql awscli
sudo systemctl restart apache2

sudo rm -rf /var/www/html
sudo aws s3 sync s3://nombre__bucket /var/www/html